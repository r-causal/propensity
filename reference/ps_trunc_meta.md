# Extract `ps_trunc` metadata

Returns the internal metadata list for a `ps_trunc` object.

## Usage

``` r
ps_trunc_meta(x)
```

## Arguments

- x:

  A **`ps_trunc`** object.

## Value

A named list of metadata.
