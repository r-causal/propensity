# Extract `ps_trim` metadata

Returns the internal metadata list for a `ps_trim` object.

## Usage

``` r
ps_trim_meta(x)
```

## Arguments

- x:

  A **`ps_trim`** object.

## Value

A named list of metadata.
