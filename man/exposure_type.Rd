% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psw.R
\name{exposure_type}
\alias{exposure_type}
\alias{density_meta}
\alias{format.propensity_density_meta}
\alias{print.propensity_density_meta}
\title{What a set of weights records about the exposure}
\usage{
exposure_type(wt)

density_meta(wt)

\method{format}{propensity_density_meta}(x, ...)

\method{print}{propensity_density_meta}(x, ...)
}
\arguments{
\item{wt}{A \code{psw} or \code{causal_wts} object.}

\item{x}{A density record, as returned by \code{density_meta()}.}

\item{...}{Not used.}
}
\value{
\itemize{
\item \code{exposure_type()}: a single string, or \code{NULL}.
\item \code{density_meta()}: a list of class \code{propensity_density_meta} with the
elements \code{density}, \code{numerator}, \code{sigma}, and \code{sigma_value}, or \code{NULL}.
\item \code{print()} and \code{format()}: the record, invisibly, and a character vector of
one line per element.
}
}
\description{
Weights carry two records describing the exposure they were built for.
\code{exposure_type()} returns the type of exposure the weight function was
given, and \code{density_meta()} returns the record left by weights whose value
is a ratio of densities.

Both describe the exposure rather than the units, so neither holds a length
of its own: both survive subsetting, arithmetic, and anything else that
changes the number of weights. Combining two sets of weights that record
either of them differently drops the record they disagree on, with a warning
of class \code{propensity_metadata_conflict_warning}. A result that no longer
records an exposure type is not described as continuous, so it drops any
density record along with it.
}
\details{
\code{exposure_type()} returns \code{"binary"}, \code{"categorical"}, or \code{"continuous"}. It
returns \code{NULL} for weights that were built without an exposure to describe,
such as those written with \code{\link[=psw]{psw()}} directly.

\code{density_meta()} returns a record of what the ratio was built from, and
\code{NULL} for weights that are not a ratio of densities, which is every set of
weights for a binary or categorical exposure:
\itemize{
\item \code{density}, the specification of the conditional density family, as built
by \code{\link[=dens_normal]{dens_normal()}} and its relatives.
\item \code{numerator}, what stabilized the weights: \code{"marginal"} for the marginal
density of the exposure, \code{"integrated"} for the conditional density
marginalized over the units, \code{"score"} for a \code{stabilization_score} the
caller supplied, and \code{"none"} for weights that were not stabilized.
\item \code{sigma}, where the residual spread of the conditional density came from:
\code{"pooled"} for the pooled residual standard deviation, and \code{"supplied"}
for a \code{.sigma} the caller gave.
\item \code{sigma_value}, the single spread the caller supplied, and \code{NULL} for a
pooled spread and for one supplied per observation. A spread that is one
number is a constant the weights can be rebuilt from, which is what
\code{\link[=ipw]{ipw()}} needs of it; a spread that changes with the observation is not,
so the record holds where it came from and nothing more.
}
}
\examples{
set.seed(1)
ps <- runif(20, 0.2, 0.8)
trt <- rbinom(20, 1, ps)

# A binary exposure is weighted by propensity scores rather than by a
# density, so there is no density to record.
exposure_type(wt_ate(ps, trt))
density_meta(wt_ate(ps, trt))

dose <- rnorm(20)
mu <- 0.3 * ps
w <- wt_ate(mu, dose, exposure_type = "continuous", stabilize = TRUE)

exposure_type(w)
density_meta(w)

}
\seealso{
\code{\link[=psw]{psw()}} for the weight vector class and the rest of what it
records, and \code{\link[=wt_ate]{wt_ate()}} for the functions that write these records.
}
