% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wt-joint.R
\name{wt_joint}
\alias{wt_joint}
\alias{is_joint_wt}
\alias{joint_wt_meta}
\title{Product weights for a joint intervention on two treatments}
\usage{
wt_joint(w_a, w_e, exposure_type = NULL)

is_joint_wt(x)

joint_wt_meta(x)
}
\arguments{
\item{w_a, w_e}{The two component weight vectors, as \code{\link[=psw]{psw()}} objects. Both
must target the \code{ate}, both must be the same length, and a component
weighting a continuous exposure must be stabilized. The order is the
factorization's order: \code{w_a} weights the first treatment and \code{w_e} weights
the second, whose model conditions on the first.}

\item{exposure_type}{A character vector of length two naming each component's
exposure type, one of \code{"binary"}, \code{"categorical"}, or \code{"continuous"}, in
the order the components were given. Defaults to the types the components
record, which is what a weight function writes on the weights it builds. A
value given here is used instead of what they record, and a component
recording no type, such as one assembled by hand, is refused unless both
types are named.}

\item{x}{An object to test, or the weights to read the record from.}
}
\value{
\code{wt_joint()} returns a \code{\link[=psw]{psw()}} vector of the elementwise product, carrying a
\code{joint_wt_meta} record.

\code{is_joint_wt()} returns a single logical.

\code{joint_wt_meta()} returns the record as a list of three elements, each one
per component and in the order the components were given, or \code{NULL} for
weights that are not a product:
\describe{
\item{\code{exposure_type}}{The two components' exposure types, in order.}
\item{\code{stabilized}}{Whether each component was stabilized, in order.}
\item{\code{density}}{Each component's density record, as \code{\link[=density_meta]{density_meta()}}
returns it, in order. A component weighting a discrete exposure is
\code{NULL}, since its weights are no ratio of densities.}
}

The record names the components rather than the observations, so it survives
subsetting and every other operation that keeps the vector a \code{\link[=psw]{psw()}}.
}
\description{
\code{wt_joint()} builds the weight for a joint intervention on two treatments by
multiplying the two component weight vectors. A joint exposure needs a weight
for the pair, and that weight factorizes sequentially: the density of the
first treatment given the covariates, times the density of the second given
the first treatment and the covariates. \code{wt_joint()} is the product, plus the
checks that make the product a joint weight rather than an arbitrary one.

\code{is_joint_wt()} reports whether a set of weights was built this way, and
\code{joint_wt_meta()} reads back what each component was.
}
\section{The factorization}{
The weight for a joint intervention on \code{A} and \code{E} is
\eqn{1 / [f(A | L) f(E | A, L)]}. The second factor conditions on the first
treatment. The product of two \emph{marginal} weights,
\eqn{1 / [f(A | L) f(E | L)]}, is a different quantity, and it is not the
joint weight for any data in which \code{E} depends on \code{A}.

Nothing downstream can tell the two apart. Both are ordinary vectors of
positive numbers, both fit an outcome model without complaint, and both
return estimates with standard errors. That is why \code{\link[=joint_wt_models]{joint_wt_models()}}
refuses a second model that does not condition on the first treatment, and
why the dependence should be modeled flexibly rather than as a single
additive term: an additive term satisfies the factorization but may model it
badly, and a badly modeled dependence biases the joint weight in a way that
is equally invisible.
}

\section{Stabilization}{
A continuous component must be stabilized. The unstabilized density ratio
\eqn{1 / f(A | L)} has a heavy right tail on its own, and multiplying it by a
second weight inherits that tail, leaving the product with no usable
variance. A continuous component is stabilized unless it was built with
\code{stabilize = FALSE}, so the requirement is met by default. A binary or
categorical component needs no stabilization and is accepted either way.

Any numerator that stabilizes the ratio satisfies the requirement, and any
density the ratio is read in is accepted: a component built with
\code{numerator = "integrated"}, with a heavier-tailed \code{.density}, or with a
\code{stabilization_score} of its own is stabilized as much as the default
marginal one is. What each component was built with is recorded per component
rather than merged, so the product carries both answers.
}

\section{What the product records}{
The result is a \code{\link[=psw]{psw()}} with estimand \code{"ate"}, since the product of two \code{ate}
weights targets the joint \code{ate}. It is marked stabilized when \emph{either}
component is, because a stabilizing numerator on either factor is a
stabilizing numerator on the product; \code{joint_wt_meta()} keeps the
per-component truth, so the coarse flag hides nothing.

The product records nothing about trimming, truncation, or calibration of the
propensity scores the components were built from. Those records name the
observations of one component, and the product is not that component.
}

\examples{
set.seed(4)
n <- 200
x1 <- rnorm(n)
a <- rbinom(n, 1, plogis(0.3 * x1))
e <- rbinom(n, 1, plogis(-0.2 + 0.5 * x1 - 0.8 * a))
dat <- data.frame(x1, a, e)

# The second model conditions on the first treatment, and does so flexibly
mod_a <- glm(a ~ x1, data = dat, family = binomial())
mod_e <- glm(e ~ a * x1, data = dat, family = binomial())

w <- wt_joint(wt_ate(mod_a), wt_ate(mod_e))
head(w)
estimand(w)
is_joint_wt(w)
joint_wt_meta(w)

# A continuous component must be stabilized, which it is by default
d <- 0.5 + 0.6 * x1 - 0.7 * a + rnorm(n)
mod_d <- lm(d ~ a * x1, data = dat)
w_d <- wt_ate(mod_d)
# Each component records the exposure type it weights, so the product knows
# which of them is the dose without being told
joint_wt_meta(wt_joint(wt_ate(mod_a), w_d))

}
\seealso{
\code{\link[=joint_wt_models]{joint_wt_models()}} for the container recording the two treatment
models, and \code{\link[=wt_ate]{wt_ate()}} for building the components. These are used by
\code{\link[=ipw]{ipw()}} methods for joint treatment models.
}
