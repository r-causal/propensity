% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipw.R
\name{print.ipw_diagnostic_se}
\alias{print.ipw_diagnostic_se}
\alias{as.data.frame.ipw_diagnostic_se}
\alias{tidy.ipw_diagnostic_se}
\title{Print a result whose standard errors are a diagnostic}
\usage{
\method{print}{ipw_diagnostic_se}(x, ...)

\method{as.data.frame}{ipw_diagnostic_se}(x, row.names = NULL, optional = FALSE, ...)

\method{tidy}{ipw_diagnostic_se}(x, ...)
}
\arguments{
\item{x}{An \code{ipw} result fit with \code{se_method = "robust"}.}

\item{...}{Passed to the next method.}

\item{row.names, optional}{Passed to the next method.}
}
\value{
\code{x}, invisibly.

\code{as.data.frame()} returns what it returns for any \code{\link[=ipw]{ipw()}} result,
carrying an \code{ipw_se_diagnostic} attribute naming the method.

\code{tidy()} returns what it returns for any \code{\link[=ipw]{ipw()}} result, carrying an
\code{ipw_se_diagnostic} attribute naming the method.
}
\description{
\code{se_method = "robust"} reports the sandwich the weighted outcome model
computes for itself, which treats the estimated weights as known. Printing
such a result writes what \link[base:print]{print()} writes for any \code{\link[=ipw]{ipw()}}
result and then one line naming the method, so that the number beside each
estimate is not read as one accounting for the propensity score model. See
\strong{Standard errors as a diagnostic} in \code{\link[=ipw]{ipw()}} for what the diagnostic does
and does not cover.
}
\examples{
set.seed(2)
n <- 300
x <- rnorm(n)
z <- rbinom(n, 1, plogis(0.4 * x))
y <- rbinom(n, 1, plogis(-0.3 + 0.7 * z + 0.5 * x))
dat <- data.frame(x, z, y)

ps_mod <- glm(z ~ x, data = dat, family = binomial())
wts <- wt_ate(ps_mod)
outcome_mod <- glm(y ~ z, data = dat, family = quasibinomial(), weights = wts)

print(ipw(ps_mod, outcome_mod, se_method = "robust"))

}
\seealso{
\code{\link[=ipw]{ipw()}} for the estimator and the standard error methods.
}
