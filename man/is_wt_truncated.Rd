% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psw.R
\name{is_wt_truncated}
\alias{is_wt_truncated}
\alias{is_unit_wt_truncated}
\title{Test whether the weights themselves have been truncated}
\usage{
is_wt_truncated(x)

is_unit_wt_truncated(x)
}
\arguments{
\item{x}{An object. \code{is_unit_wt_truncated()} accepts only a \link{psw} vector.}
}
\value{
\itemize{
\item \code{is_wt_truncated()}: a single \code{TRUE} or \code{FALSE}. It is \code{FALSE} for
anything that is not a \link{psw} vector.
\item \code{is_unit_wt_truncated()}: a logical vector the same length as \code{x}, \code{TRUE}
for each weight the bound moved.
}
}
\description{
\code{is_wt_truncated()} returns \code{TRUE} for a \link{psw} vector whose own values were
bounded, and \code{is_unit_wt_truncated()} returns which of its weights the bound
moved.

Truncating the weights is recorded apart from truncating the propensity
scores they were built from, which \code{\link[=is_ps_truncated]{is_ps_truncated()}} and
\code{\link[=is_unit_truncated]{is_unit_truncated()}} report. The two are different operations, and a set of
weights can carry either, both, or neither.
}
\details{
The truncation leaves a record on the weights, the \code{psw_trunc_meta}
attribute, holding the method, the bound as given and as applied, and the
positions of the weights it moved. \code{is_unit_wt_truncated()} answers from
those positions, so the record follows the rules \link{psw} describes for the
records a modified propensity score leaves: it is kept through arithmetic
and subassignment, re-indexed through the subscript of \code{[}, and loses its
positions to any other slice and to a combine, which keeps the method and
bounds. The \code{wt_truncated} flag describes the weights as
a whole and is kept through all of those, so \code{is_wt_truncated()} keeps its
answer where \code{is_unit_wt_truncated()} has none to give.

\code{is_unit_wt_truncated()} therefore checks that the record covers the weights
it is given, and raises an error of class \code{propensity_missing_meta_error}
when it does not, or when weights marked as truncated carry no record at
all, rather than name truncated weights at stale positions. Weights that
were not truncated have no record to read, and every weight is reported as
untouched.
}
\examples{
w <- psw(c(1.2, 0.8, 1.5), estimand = "ate")
is_wt_truncated(w)
is_unit_wt_truncated(w)

}
\seealso{
\link{psw} for the weight vector class, and \code{\link[=is_ps_truncated]{is_ps_truncated()}} for
truncation of the propensity scores.
}
