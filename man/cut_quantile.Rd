% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{cut_quantile}
\alias{cut_quantile}
\title{Cut Numeric Vector into Quantile-Based Intervals}
\usage{
cut_quantile(x, probs = seq(0, 1, 0.1), include.lowest = TRUE, ...)
}
\arguments{
\item{x}{A numeric vector to be cut into intervals.}

\item{probs}{A numeric vector of probabilities with values in \link{0,1}
giving the quantiles to use as breakpoints. Default creates deciles.}

\item{include.lowest}{Logical, indicating if an 'x\link{i}' equal to the
lowest (or highest, for right = FALSE) 'breaks' value should be
included. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[=cut]{cut()}}.}
}
\value{
A factor with levels corresponding to the quantile-based intervals.
}
\description{
\code{cut_quantile()} is a wrapper around \code{\link[=cut]{cut()}} that uses quantiles as
breakpoints. By default, it creates 10 intervals based on deciles
(quantiles at 0, 0.1, 0.2, ..., 1.0).
}
\examples{
set.seed(123)
x <- rnorm(100)

# Default: cut into deciles
cut_quantile(x)

# Custom quantiles: cut into quartiles
cut_quantile(x, probs = seq(0, 1, 0.25))

# Cut into tertiles
cut_quantile(x, probs = c(0, 1/3, 2/3, 1))

}
