% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipw-tidiers.R
\name{glance.ipw_pooled}
\alias{glance.ipw_pooled}
\title{Glance at a pooled inverse probability weighted result}
\usage{
\method{glance}{ipw_pooled}(x, ...)
}
\arguments{
\item{x}{An \code{ipw_pooled} object, as returned by \code{\link[=pool_ipw]{pool_ipw()}}.}

\item{...}{These dots are for future extensions and must be empty.}
}
\value{
A one-row \link[tibble:tibble]{tibble} with the columns:
\describe{
\item{\code{estimand}}{The causal estimand every pooled result targeted.}
\item{\code{nobs}}{The smallest number of observations any pooled result was
estimated from. The imputed datasets are the same size, so this is that
size unless a result was fitted on a subset of one. Reported by
\code{\link[stats:nobs]{stats::nobs()}}.}
\item{\code{m}}{The number of results pooled.}
\item{\code{dfcom}}{The complete-data degrees of freedom the Barnard-Rubin
adjustment used.}
}
}
\description{
\code{glance()} describes a \code{\link[=pool_ipw]{pool_ipw()}} result rather than its estimates: one row
naming the estimand, counting the observations and the results that were
pooled, and reporting the complete-data degrees of freedom the small-sample
adjustment used. A pooled result reporting several effect measures, or several
contrasts of a categorical exposure, still returns exactly one row.
}
\examples{
\dontshow{if (requireNamespace("mice", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(2024)
n <- 150
x1 <- rnorm(n)
z <- rbinom(n, 1, plogis(0.3 * x1))
y <- rbinom(n, 1, plogis(-0.4 + 0.9 * z + 0.5 * x1))
dat <- data.frame(x1, z, y)
dat$x1[rbinom(n, 1, 0.2) == 1] <- NA

imp <- mice::mice(dat, m = 2, print = FALSE, seed = 1)
fits <- with(imp, {
  ps <- glm(z ~ x1, family = binomial())
  w <- wt_ate(ps)
  om <- glm(y ~ z, family = quasibinomial(), weights = w)
  ipw(ps, om)
})

glance(pool_ipw(fits))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=pool_ipw]{pool_ipw()}} for the pooling, \code{\link[=tidy.ipw_pooled]{tidy()}} for the
pooled estimates, and the Multiple imputation section of \code{\link[=ipw]{ipw()}} for the
workflow the two belong to.
}
