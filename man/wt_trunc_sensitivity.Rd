% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wt_trunc_sensitivity.R
\name{wt_trunc_sensitivity}
\alias{wt_trunc_sensitivity}
\title{Tabulate Truncated Weights Over a Grid of Bounds}
\usage{
wt_trunc_sensitivity(
  .weights,
  method = c("pctl", "wt", "count"),
  lower = NULL,
  upper = NULL
)
}
\arguments{
\item{.weights}{A \link{psw} vector, such as one returned by \code{\link[=wt_ate]{wt_ate()}}, or a
numeric vector of weights. Propensity scores, including those returned by
\code{\link[=ps_trim]{ps_trim()}}, \code{\link[=ps_trunc]{ps_trunc()}}, and \code{\link[=ps_calibrate]{ps_calibrate()}}, are refused with an error
of class \code{propensity_type_error}, and weights that have already been
truncated with an error of class \code{propensity_already_modified_error}.}

\item{method}{The rule that reads each bound, one of \code{"pctl"} (the
default), \code{"wt"}, or \code{"count"}, as in \code{\link[=wt_trunc]{wt_trunc()}}. The \code{"adaptive"}
method sets a single bound from the number of weights, so it has no grid
to vary and is not accepted.}

\item{lower}{An optional grid of lower bounds, read according to \code{method}
as \code{wt_trunc()} reads \code{lower}. A single value is used with every upper
bound; otherwise \code{lower} must have the same length as \code{upper}, and the two
are paired element by element.}

\item{upper}{The grid of upper bounds, read according to \code{method} as
\code{wt_trunc()} reads \code{upper}. For \code{"pctl"} it defaults to
\code{c(0.99, 0.975, 0.95, 0.90)}; for \code{"wt"} and \code{"count"} it is required.}
}
\value{
A \link[tibble:tibble]{tibble} with one row for the untruncated weights
followed by one row per bound, and the columns:
\itemize{
\item \code{lower}, \code{upper}: the bounds as given, \code{NA} in the first row and \code{lower}
\code{NA} when no lower bound was given.
\item \code{lower_value}, \code{upper_value}: the bounds as applied, on the scale of the
weights, recorded by \code{wt_trunc()}. Both are \code{NA} in the first row, and
\code{lower_value} is \code{NA} when no lower bound was given.
\item \code{n_truncated}: the number of weights moved, 0 in the first row.
\item \code{min}, \code{max}, \code{mean}: the smallest, largest, and mean weight present.
\item \code{range_ratio}: \code{max / min}.
}
}
\description{
\code{wt_trunc_sensitivity()} truncates a set of weights with \code{\link[=wt_trunc]{wt_trunc()}} at
each bound in a grid and tabulates what each truncation does to them: the
bound as given and as applied, the number of weights moved, and the range
and mean of the weights that result. The first row describes the weights
before any truncation, so every other row can be read against it. This is
the weight side of the sensitivity table Cole and Hernán (2008) recommend
for choosing a bound.
}
\details{
Each row after the first is computed by calling \code{\link[=wt_trunc]{wt_trunc()}} with that
row's bounds, so it describes exactly the weights that call returns. Every
bound in the grid must be one \code{wt_trunc()} accepts: a single invalid bound
refuses the whole grid, with the error \code{wt_trunc()} would raise for it,
rather than leaving a gap in the table. The rows follow the grid in the
order given.

The summaries are taken over the weights that are present, so missing
weights, such as those of units set aside by \code{\link[=ps_trim]{ps_trim()}}, take no part in
them and are never counted as truncated. A grid over weights of which none
are present is refused with an error of class \code{propensity_range_error}.
Weights can be zero, and a zero weight makes \code{range_ratio} infinite.

Weights that have already been truncated are refused, because the first
row would then describe weights that were already bounded rather than the
weights before truncation. Pass the weights as they were before
\code{wt_trunc()} instead.
\subsection{What is left to the caller}{

The table does not include effect estimates or effective sample sizes.
propensity fits no outcome models, so to add estimates to the table, map
over the grid, truncate the weights at each bound with \code{wt_trunc()}, and fit
the outcome model with each set of weights. For the effective sample size,
add the weights before and after truncation to a data frame as separate
columns and pass them to \code{halfmoon::check_ess()}.
}
}
\examples{
set.seed(1)
n <- 200
x <- rnorm(n)
z <- rbinom(n, 1, plogis(2 * x))
fit <- glm(z ~ x, family = binomial)
w <- wt_ate(fit, .exposure = z)

# The default percentile grid. The first row is the untruncated weights.
wt_trunc_sensitivity(w)

# A grid of absolute bounds, and a lower tail bounded at every one of them
wt_trunc_sensitivity(w, method = "wt", upper = c(20, 10, 5))
wt_trunc_sensitivity(w, method = "wt", lower = 1.05, upper = c(20, 10, 5))

# The largest one, three, and ten weights
wt_trunc_sensitivity(w, method = "count", upper = c(1, 3, 10))

}
\references{
Cole, S. R., & Hernán, M. A. (2008). Constructing inverse probability
weights for marginal structural models. \emph{American Journal of
Epidemiology}, 168(6), 656--664.
}
\seealso{
\code{\link[=wt_trunc]{wt_trunc()}} to truncate the weights at a chosen bound.
}
