% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density.R
\name{dens_normal}
\alias{dens_normal}
\alias{dens_laplace}
\alias{dens_t}
\alias{dens_kernel}
\alias{dens_fn}
\title{Density specifications for continuous exposures}
\usage{
dens_normal()

dens_laplace()

dens_t(df)

dens_kernel(bw = "nrd0", adjust = 1, kernel = "gaussian", n = 512)

dens_fn(f)
}
\arguments{
\item{df}{Degrees of freedom for Student's t, a single positive, finite
number.}

\item{bw}{The bandwidth passed to \code{\link[stats:density]{stats::density()}}: a single positive
number, or the name of one of its selection rules
(\code{"nrd0"}, \code{"nrd"}, \code{"ucv"}, \code{"bcv"}, \code{"SJ"}, \code{"SJ-ste"}, or \code{"SJ-dpi"}).}

\item{adjust}{A single positive number the bandwidth is multiplied by, as in
\code{\link[stats:density]{stats::density()}}. Values above 1 smooth the estimate further.}

\item{kernel}{The smoothing kernel, one of the kernels \code{\link[stats:density]{stats::density()}}
accepts.}

\item{n}{The number of grid points \code{\link[stats:density]{stats::density()}} evaluates, at least 2.
The estimate is interpolated between them, so a larger \code{n} follows the
shape of the residuals more closely.}

\item{f}{A function of one argument, the standardized residual, returning one
non-negative, finite density value for each element it is given.}
}
\value{
An object of class \code{propensity_density}: a list with the elements
\code{family}, \code{params}, and \code{fn}. \code{fn} is the function that evaluates the
density, and is \code{NULL} for \code{dens_kernel()}.
}
\description{
Weights for a continuous exposure are a ratio of densities. Both densities
are evaluated on the standardized residual
\eqn{z_i = (A_i - \mu_i) / \sigma}, where \eqn{A_i} is the exposure,
\eqn{\mu_i} the fitted conditional mean, and \eqn{\sigma} the residual
spread, so a specification describes a density on a standardized scale rather
than on the scale of the exposure itself.

These constructors name the family and record the parameters that identify
it. Pass one to the \code{.density} argument of \code{\link[=wt_ate]{wt_ate()}} or \code{\link[=wt_cens]{wt_cens()}}, which
also accept the strings \code{"normal"}, \code{"laplace"}, and \code{"kernel"} for the
families that need no parameters, and a bare function of one argument, which
is wrapped with \code{dens_fn()}.
\itemize{
\item \code{dens_normal()} is the standard normal density, the default.
\item \code{dens_laplace()} is the standard Laplace density,
\eqn{\exp(-|z|) / 2}, which puts more mass in the tails than the normal.
\item \code{dens_t()} is Student's t density with \code{df} degrees of freedom, heavier
tailed still, and heavier the smaller \code{df} is.
\item \code{dens_kernel()} is a kernel density estimate of the standardized
residuals, fit with \code{\link[stats:density]{stats::density()}} and interpolated to each
observation. It assumes no family at all, at the cost of a density that
is not a smooth function of the model's parameters: weights built from it
have no closed-form standard error.
\item \code{dens_fn()} is a density you write yourself.
}
}
\examples{
dens_normal()

dens_t(df = 4)

dens_kernel(adjust = 1.5)

dens_fn(function(z) stats::dt(z, df = 4))

}
