# the weight-truncation refusal names both honest routes

    Code
      expr
    Condition <propensity_ipw_wt_truncated_error>
      Error in `ipw()`:
      ! `outcome_mod` was fit with weights truncated on the weight scale with `wt_trunc()`.
      x `ipw()` cannot yet account for truncated propensity score weights: a hard bound is not a smooth function of the propensity score model's parameters, so the stacked sandwich would report an interval for the truncated estimand without saying so.
      i Rebuild the weights without `wt_trunc()` and report the M-estimation interval, or fit `outcome_mod` with the truncated weights and report the fixed-weight sandwich yourself, stating that it conditions on the bound.

# the weight-truncation refusal reads the same on every route

    Code
      expr
    Condition <propensity_ipw_wt_truncated_error>
      Error in `ipw()`:
      ! `outcome_mod` was fit with weights truncated on the weight scale with `wt_trunc()`.
      x `ipw()` cannot yet account for truncated propensity score weights: a hard bound is not a smooth function of the propensity score model's parameters, so the stacked sandwich would report an interval for the truncated estimand without saying so.
      i Rebuild the weights without `wt_trunc()` and report the M-estimation interval, or fit `outcome_mod` with the truncated weights and report the fixed-weight sandwich yourself, stating that it conditions on the bound.

---

    Code
      expr
    Condition <propensity_ipw_wt_truncated_error>
      Error in `ipw()`:
      ! `outcome_mod` was fit with weights truncated on the weight scale with `wt_trunc()`.
      x `ipw()` cannot yet account for truncated propensity score weights: a hard bound is not a smooth function of the propensity score model's parameters, so the stacked sandwich would report an interval for the truncated estimand without saying so.
      i Rebuild the weights without `wt_trunc()` and report the M-estimation interval, or fit `outcome_mod` with the truncated weights and report the fixed-weight sandwich yourself, stating that it conditions on the bound.

---

    Code
      expr
    Condition <propensity_ipw_wt_truncated_error>
      Error in `ipw()`:
      ! `outcome_mod` was fit with weights truncated on the weight scale with `wt_trunc()`.
      x `ipw()` cannot yet account for truncated propensity score weights: a hard bound is not a smooth function of the propensity score model's parameters, so the stacked sandwich would report an interval for the truncated estimand without saying so.
      i Rebuild the weights without `wt_trunc()` and report the M-estimation interval, or fit `outcome_mod` with the truncated weights and report the fixed-weight sandwich yourself, stating that it conditions on the bound.

